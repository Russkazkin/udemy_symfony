{% extends 'base.html.twig' %}

{% block body %}
    {% for post in posts %}
    <div class="media d-flex text-muted pt-3">
        <img data-src="holder.js/32x32?text=MJ&bg=e83e8c&fg=fff&size=8" alt="" class="d-block rounded">
        <p class="media-body pb-3 mb-0 ms-2 small lh-125 border-bottom border-gray">
            <span class="d-block"><strong class="text-gray-dark">@username</strong> <small>· {{ post.time|date("d/m/Y") }}</small></span>
            {{ post.text }}
        </p>
    </div>
    {% endfor %}
{% endblock %}
